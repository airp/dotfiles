local options = {
  ensure_installed = {
    "luadoc",
    "html",
    "css",
    "go",
  },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
